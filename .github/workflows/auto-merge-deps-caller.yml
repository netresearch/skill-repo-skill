name: Auto-merge dependency PRs

on:
  pull_request:

permissions: {}

jobs:
  auto-merge:
    uses: ./.github/workflows/auto-merge-deps.yml
    permissions:
      contents: write
      pull-requests: write
